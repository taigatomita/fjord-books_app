<%= form_with(model: [commentable, Comment.new]) do |form| %>

  <div class="field">
    <%= form.text_area :content, required: :required %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
